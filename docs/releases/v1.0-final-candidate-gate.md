# v1.0 final release-candidate gate

Status: passed. The complete pre-publication ladder ran against exact commit
`91e53c1a9c790f32aa16b9ab57a19a4a76c3c849` from PR
[#228](https://github.com/El37628/FluxFast/pull/228).

That commit is based on the reviewed 1.0.0 version-promotion merge
`b53d6f0c928260052bb6320fa7986ea1078a273d`. Its only changes repair the
comparison-workflow manifest alignment and add the corresponding regression
test and documentation. No Core, Next, or Python runtime source changed. The
version-normalized digest still covers 100 runtime files and remains:

```text
cb839991228a6ca12b5341b1c63d0602ed5b559250708f31cbbcd5eb0bc3d762
```

The PR rollup completed 61 of 61 checks successfully, with no pending, skipped,
cancelled, or failed check. The recorded workflow runs are immutable evidence
for the exact commit:

| Gate | Exact-commit evidence |
| --- | --- |
| Python 3.11, 3.12, 3.13, 3.14 and Python unit tests | [Python run 36025771115](https://github.com/El37628/FluxFast/actions/runs/36025771115) |
| Node.js 22/24, unit tests, typecheck, generated drift, schema compatibility | [JavaScript run 36025771165](https://github.com/El37628/FluxFast/actions/runs/36025771165) |
| Browser E2E, production E2E, Redis 6.2/8.10, multi-worker Redis | [Integration run 36025771145](https://github.com/El37628/FluxFast/actions/runs/36025771145) |
| Docker, rootless Podman, Docker Compose, rootless Podman Compose | [Container run 36025771185](https://github.com/El37628/FluxFast/actions/runs/36025771185) |
| Minimum/latest Next 16 and React 19, packed consumers, v0.9/1.0 mixed upgrades and rollback, exact four-distribution contract | [Release-artifacts run 36025771201](https://github.com/El37628/FluxFast/actions/runs/36025771201) |
| CodeQL for Python and JavaScript/TypeScript | [CodeQL run 36025797591](https://github.com/El37628/FluxFast/actions/runs/36025797591) |
| Dependency review, strict pip-audit, and pnpm high/critical audit | [Dependency-security run 36025797484](https://github.com/El37628/FluxFast/actions/runs/36025797484) |
| Controlled v0.9/candidate comparison in both workload orders | [Benchmark run 36025792262](https://github.com/El37628/FluxFast/actions/runs/36025792262) |

The pull-request event repeated the language, integration, container, and
release-artifact workflows on the same head. GitGuardian also passed in the
61-check rollup. This duplication is not substituted for any required context;
all contexts completed successfully.

## Version and consumer matrix

The exact candidate retained synchronized version `1.0.0` across Python, Core,
Next, the root metadata, the Next-to-Core range, and the lock. The release
artifact workflow exercised:

| Python package | JavaScript packages | Result |
| --- | --- | --- |
| 0.9.0 | 0.9.0 | published stable baseline passed |
| 1.0.0 candidate | 0.9.0 | Python-first rollout passed |
| 0.9.0 | 1.0.0 candidate | JavaScript-first rollout passed |
| 1.0.0 candidate | 1.0.0 candidate | matched packed candidate passed |
| rollback to 0.9.0 | rollback to 0.9.0 | same-project rollback passed |

The candidate matrix also installed and exercised the minimum supported
Next.js 16.3.0/React 19.0.0 combination on Node 22 and the latest compatible
Next.js 16/React 19 combination on Node 24. The Python matrix covered 3.11
through 3.14. Redis compatibility covered 6.2.24 and 8.10.1, in addition to the
three-worker production topology and shared-cache/live-convergence checks.

## Benchmark evidence

The first promoted-candidate attempt correctly failed before measurement: the
v0.9 checkout received the current lock but not the promoted root/Core
manifests, so pnpm rejected the frozen install. Commit `91e53c1` aligns all five
lock-coupled metadata files while the comparison runner permits exactly those
files and continues to reject any baseline runtime-source change.

The successful run uploaded artifact `v090-candidate-comparison` (artifact ID
`10820825000`). Both `comparison.json` records are complete and identify:

- baseline tag commit `ee50ce0276c713a066e9324e3a9e5d2fc6df3872`;
- candidate commit `91e53c1a9c790f32aa16b9ab57a19a4a76c3c849`;
- lock digest `b2fb5172226a9507620c9a1da2ccae3c60bb4a5342fd43a4cffba03b1cb5e6ce`;
- 18 results in each order, with 17 passes, the narrowly recognized historical
  v0.9 state-only memory defect, and zero failed candidate workloads; and
- exact runtime and benchmark-harness hashes checked before and after each run.

Request/cache, deferred, live, Redis, schema/code generation, validation,
memory, bundle, and production-topology workloads all retained their mandatory
correctness properties. Timing and heap measurements remain observations under
the documented performance policy; no new release blocker was found.

## Freeze decision

There are zero known unresolved P0/P1 findings. The final gate found and fixed a
benchmark-orchestration defect, not a runtime defect. Evidence-only documentation
added after the exact run does not affect the validated runtime, package
metadata, lock, workflow, benchmark harness, protocol, or schema. Any later
change to one of those inputs requires the affected release validation to run
again before tagging.
