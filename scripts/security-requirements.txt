# Keep synchronized with [project].dependencies and the redis/dev optional
# dependency groups in python/fluxfast/pyproject.toml. Listing dependencies
# directly lets the pre-publication 1.0 candidate be audited before fluxfast
# 1.0.0 exists on PyPI.
fastapi>=0.141.1,<1.0.0
pydantic>=2.9.0
anyio>=4.0.0
starlette>=1.3.1,<2.0.0
uvicorn>=0.30.0,<1.0.0
redis>=5.0.0,<9.0.0
httpx2>=2.0.0,<3.0.0
pytest>=8.0.0
ruff>=0.3.0
